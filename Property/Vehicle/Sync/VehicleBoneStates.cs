namespace GM.Property.Vehicle.Sync
{
    public enum DoorState
    {
        Closed,
        Open,
        Broken,
    }
    
    public enum WheelState
    {
        Fixed,
        Burst,
        OnRim,
    }
}